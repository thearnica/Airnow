import {QSA} from "./global";import {openPopupThankYou} from "./modal";function getValues(form) {  const inputs = QSA(".airnow-field__input", form);  let values = {};  inputs.forEach(function (input) {    values[input.getAttribute('name')] = input.value;  });  return values;}document.querySelector('.airnow-contact__form').addEventListener('submit', function (event) {  let values = getValues(event.target);  // как-то отправить данные куда-то  openPopupThankYou();  event.preventDefault();  return false;});